AlarmIntent:
  action:
    - service: input_boolean.turn_on
      entity_id: input_boolean.alarm_clock
    - service: input_select.select_option
      data_template:
        entity_id: input_select.alarm_clock
        option: strptime(Alarmtime, "%H:%M")
  speech:
    type: plaintext
    text: !include alexa_confirm.yaml
