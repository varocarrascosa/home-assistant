default_view:
  view: yes
  entities:
    #- group.alarm_clock
    - group.scripts
    - group.meds_reminder
    - device_tracker.ios_alvaro
    - device_tracker.ios_ashlee
    - sensor.moon
    - group.weather_card_forecast
    - sensor.eta_homebrandeis
    - sensor.eta_homebu
alltrackers:
  entities:
    - group.alltrackers
    - device_tracker.ios_ashlee
alvaro_tracker:
  entities:
    - device_tracker.ios_alvaro
    - device_tracker.iphone
switches:
  entities:
    - switch.living_room_tv
    - switch.bedroom_tv
    - switch.neo_coolcam_power_plug_12a_switch
lands:
  view: yes
  entities:
      - group.all_lights
      - group.switches
Surveillance:
  view: yes
  entities:
Media:
  view: yes
  entities:
    - media_player.kitchen
    - media_player.bedroom_2
scripts:
  entities:
    - script.night_music
    - script.update_config
    - script.test
cars:
  entities:
    #- sensor.eta_homeschool
    #- device_tracker.beacon_varocar
alarm_clock:
  name: Alarm Clock
  entities:
    - sensor.alarm_clock_time
    - input_number.alarm_clock_hour
    - input_number.alarm_clock_minutes
    - input_boolean.alarm_clock_status
volume_control:
  icon: mdi:music-note-eighth
  entities:

meds_reminder:
    entities:
      - input_boolean.meds_taken
      - input_boolean.meds_reminder
