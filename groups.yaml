default_view:
  view: yes
  entities:
    - group.scripts
    - group.alarm_clock
Surveillance:
  view: yes
  entities:
Media:
  view: yes
  entities:
    - media_player.kitchen
alvaro_lights:
  icon: mdi:light
  entities:
scripts:
  entities:
    #- script.night_music
    - script.rap
    - script.update_config
cars:
  entities:
    #- sensor.eta_homeschool
    #- device_tracker.beacon_varocar
alarm_clock:
  name: Alarm Clock
  entities:
    - input_boolean.alarm_clock_status
    - sensor.alarm_clock_time
    - input_slider.alarm_clock_hour
    - input_slider.alarm_clock_minute
    - input_slider.alarm_clock_fade_in_minutes
volume_control:
  icon: mdi:music-note-eighth
  entities:
