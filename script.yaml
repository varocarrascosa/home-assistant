update_config:
  alias: 'Update & Restart'
  sequence:
    - service: shell_command.get_latest_config
    - delay: 00:00:05
    - service: homeassistant.restart

say:
  alias: SONOS TTS
  sequence:
    - service: media_player.sonos_snapshot
      data_template:
        entity_id: "{{ where }}"
    - service: tts.google_say
      entity_id: "{{ where }}"
      data_template:
        message: "{{ what }}"
    - service: media_player.sonos_restore
      data_template:
        entity_id: "{{ where }}"

night_music:
  alias: 'Music Night'
  sequence:
    - service: media_player.volume_set
      data:
        entity_id: media_player.kitchen
        volume_level: .4
    - service: tts.google_say
      entity_id: media_player.kitchen
      data:
        message: 'Goodnight guys, I will play some music for a little'
    - delay: 00:00:03
    - service: media_player.select_source
      data:
        entity_id: media_player.kitchen
        source: 'Migos'
    - service: media_player.volume_set
      data:
        entity_id: media_player.kitchen
        volume_level: .15
    - delay: 01:00:00
    - service: media_player.media_pause
      data:
        entity_id: media_player.kitchen

rap:
  alias: 'Rap'
  sequence:
    - service: script.say
      data:
        variables:
          where: media_player.kitchen
          what: 'Tej is gay'
    - delay: 00:00:01
    - service: media_player.select_source
      data:
        entity_id: media_player.kitchen
        source: 'Migos'
    - service: media_player.volume_set
      data:
        entity_id: media_player.kitchen
        volume_level: .4
